# MCPress MCP Server - Environment Variables
# Copy to .env and set MCPRESS_BACKEND_URL if the backend is not on localhost:8000.

# Backend API URL (embeddings and search run on the backend)
MCPRESS_BACKEND_URL=http://localhost:8000

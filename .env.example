# =============================================================================
# MCPress – root .env.example (reference only)
# Each part (backend, frontend, mcp-server) has its own .env; see their .env.example.
# =============================================================================

# Backend API URL – used by:
#   - backend/scripts/populate_articles.py (when run from repo root)
#   - mcp-server (set in mcp-server/.env as MCPRESS_BACKEND_URL)
BACKEND_URL=http://localhost:8000

# Frontend – set in frontend/.env (NEXT_PUBLIC_* are read by the Next.js app)
# NEXT_PUBLIC_API_URL=http://localhost:8000   # Backend API for articles
# NEXT_PUBLIC_APP_URL=http://localhost:3000

# MCP Server – set in mcp-server/.env (server calls backend API)
# MCPRESS_BACKEND_URL=http://localhost:8000

# Optional: only if the frontend chat connects to an MCP server over HTTP/SSE
# MCP_SERVER_URL=http://localhost:3001/sse
# MCP_SERVER_TRANSPORT=sse

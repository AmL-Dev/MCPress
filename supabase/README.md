# Supabase (deprecated)

Storage for MCPress is now **Chroma** (local vector store). This folder is kept for reference only.

The previous Supabase/PostgreSQL migrations have been archived in `docs/deprecated-supabase-migrations/`. They are no longer applied or used.
